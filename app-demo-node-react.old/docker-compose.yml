version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3030:3000"
    environment:
      - NODE_ENV=development
  frontend:
    build: ./frontend
    ports:
      - "8090:80"
    depends_on:
      - backend
