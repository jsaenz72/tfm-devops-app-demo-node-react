apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-delivery-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  delivery.json: |
    { ... contenido del dashboard Delivery ... }
