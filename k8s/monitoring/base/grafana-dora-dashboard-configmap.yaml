apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dora-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  dora-metrics.json: |
    { ... contenido del dashboard DORA ... }
